
version: 0.1
phases:
  install:
    commands:
      - npm install time
      - aws cloudformation package --template-file template.yml --s3-bucket ppl-lambda-example 
                                   --output-template-file NewSamTemplate.yaml
artifacts:
  type: zip
  files:
    - template.yaml
